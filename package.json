{
  "name": "ws-rest",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/@node-novel/*",
    "packages/*"
  ],
  "scripts": {
    "lerna:prepublish": "lerna run --scope @node-novel/cached-dmzj build",
    "lerna": "yarn run lerna:prepublish && lerna publish",
    "ncu": "npx npm-check-updates -u",
    "sort-package-json": "npx \"sort-package-json\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@bluelovers/tsconfig": "^1.0.3",
    "@types/fs-extra": "^7.0.0"
  },
  "resolutions": {},
  "dependencies": {
    "fs-extra": "^8.0.1"
  }
}
