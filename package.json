{
  "name": "ws-rest",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/@node-novel/*",
    "packages/@bluelovers/*",
    "packages/*"
  ],
  "scripts": {
    "lerna:prepublish": "lerna run --scope @node-novel/cached-* build",
    "lerna": "yarn run lerna:prepublish && lerna publish --yes --cd-version patch",
    "ncu": "npx npm-check-updates -u",
    "sort-package-json": "npx \"sort-package-json\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@bluelovers/tsconfig": "^1.0.3",
    "@types/fs-extra": "^7.0.0"
  },
  "resolutions": {
    "axios": "< 0.19 && >= 0.18"
  },
  "dependencies": {
    "@bluelovers/fast-glob": "^3.0.4",
    "@ts-type/package-dts": "^1.0.4",
    "fs-extra": "^8.1.0",
    "upath2": "^2.1.3",
    "workspaces-config": "^1.0.3"
  }
}
